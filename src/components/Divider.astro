---
const {size, margin} = Astro.props;
---

<div class={`divider divider-${size} divider-${margin}`}></div>
<style>
  .divider {
    margin: 0 auto;
    width: 100%;
    height: 1px;
    background-color: var(--accent);
  }
</style>
