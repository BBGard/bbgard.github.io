---
// const Navbar = {
//   logo: "/path/to/logo.png",
//   links: [
//     // { text: "About", url: "/#about" },
//     // { text: "Projects", url: "/#projects" },
//     // { text: "Contact", url: "/#contact" },
//     // { text: "Dev.Log", url: "/blog" },
//   ],
// };
---

<link
  rel="stylesheet"
  href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200"
/>

<nav class="navbar fade-in">
  <div class="nav-container">
    <div class="logo">
      <a class="logo-text" href="/">Benjamin Gardiner</a>
    </div>

    <div class="hamburger">
      <svg
        class="menu-icon"
        xmlns="http://www.w3.org/2000/svg"
        width="2em"
        height="2em"
        viewBox="0 0 16 16"
        ><path fill="black" d="M15 4H1V2h14zm0 5H1V7h14zM1 14h14v-2H1z"
        ></path></svg
      >
      <svg
        class="close-icon"
        xmlns="http://www.w3.org/2000/svg"
        width="2em"
        height="2em"
        viewBox="0 0 16 16"
        ><path
          fill="black"
          d="m3.5 2.086l4.5 4.5l4.5-4.5L13.914 3.5L9.414 8l4.5 4.5l-1.414 1.414l-4.5-4.5l-4.5 4.5L2.086 12.5l4.5-4.5l-4.5-4.5z"
        ></path></svg
      >
    </div>
  </div>
</nav>
<div class="slide-out-menu">
  <ul class="nav-links">
    <li>
      <a class="email-text" href="mailto:ben.b.gardiner@gmail.com"
      >ben.b.gardiner@gmail.com</a
    >
    </li>

  </ul>
</div>

<style>
  /* Navbar Styles */

  .navbar {
    position: fixed;
    top: 0;
    padding-top: 1rem;
    width: 100%;
    height: 4.5rem;
    background-color: var(--background-light);
    color: var(--black-100);
    box-shadow: 2px 2px 0px var(--background-dark-transparent);
    transition: all 0.5s;
    z-index: 2;
  }

  .nav-container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    width: 100%;
    max-width: 2000px;
    margin: 0 auto;
    padding: 0 2rem 1rem 2rem;
  }

  .navbar .logo .logo-text,
  .nav-links .email-text {
    font-size: var(--heading-4);
    font-family: var(--font-logo);
    font-weight: 600;
    text-decoration: none;
    color: var(--white-100);
    background-color: var(--black-100);
    padding: 0 0.5rem;
    transition: all 0.2s ease-in-out;
  }

  .navbar .logo .logo-text:hover,
  .nav-links .email-text:hover {
    background-color: var(--white-100);
    color: var(--black-70);
    box-shadow: inset 2px 2px 0px var(--background-dark-transparent);
  }


  .slide-out-menu {
    position: fixed;
    top: 4.5rem;
    left: 100vw;
    width: 50vw;
    height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: flex-start;
    align-items: center;
    padding-top: 6rem;
    box-shadow: -2px 2px 0px var(--background-dark-transparent);
    background-color: var(--background-light);
    z-index: 1;
    transition: all 0.2s ease-in-out;
  }
  .slide-out-menu.expanded {
    left: 49vw;
  }


  .nav-links {
    padding: 0;
  }

  .nav-links a {
    font-size: var(--heading-4);
    font-family: var(--font-logo);
    font-weight: 600;
    text-decoration: none;
    color: var(--white-100);
    background-color: var(--black-100);
    padding: 0 0.5rem;
    transition: all 0.2s ease-in-out;
  }



  .hide-nav {
    top: -5rem;
  }

  /* Hamburger */
  .hamburger {
    display: block;
    cursor: pointer;
    z-index: 2;
  }
  .menu-icon {
    display: block;
  }

  .close-icon {
    display: none;
  }

  .hamburger.is-active .menu-icon {
    display: none;
  }

  .hamburger.is-active .close-icon {
    display: block;
  }

  @media (max-width: 991px) {
    .navbar .email .email-text {
      display: none;
    }

    .email-text.expanded {
      display: flex;
    }

    /* Make slide out menu fullscreen on tablet and mobile */
    .slide-out-menu {
      width: 100vw;
      left: 100vw;
    }

    .slide-out-menu.expanded {
      left: 0;
    }

  }

  @media (max-width: 768px) {
    .navbar .logo .logo-text,
    .slide-out-menu .nav-links a {
      font-size: var(--heading-6);
    }
  }


</style>
